@model SMSDataContract.Common.DailyAssessmentHelper
@{
    ViewBag.Title = "AddChangesAssessmentReport";
    Layout = "~/Views/Shared/_NewLayout.cshtml";
}

<div class="row">
    <div class="col-md-12 col-sm-12">
        <div class="well">
            <div id="stdBasicInfo">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <div class="row">
                        <div class="col-md-12 col-sm-12">
                            @*@foreach (var parent in Model.AssessmentType)
                                {

                                    <table>
                                        <tr>

                                            <td>
                                                @Html.Hidden("AssessmentId", new { parent.AssessmentTypeId })
                                                @parent.AssessmentName
                                            </td>
                                        </tr>

                                        @foreach (var child in Model.AssessmentSubType)
                                        {
                                            <text>
                                                @if (parent.AssessmentTypeId == child.AssessmentTypeId)
                                                {   <text>
                                                    <tr>
                                                        <td>
                                                            @Html.Hidden("AssessmentSubTypeId", new { child.AssessmentSubTypeId })
                                                            @child.AssessmentSubTypeName
                                                        </td>

                                                        <td>
                                                            <input type="radio" name="DynamicCategoryItems[@child]" value="@child.VeryGood" @(child.VeryGood ? "checked" : "") />

                                                        </td>
                                                        <td>

                                                            <input type="radio" name="DynamicCategoryItems[@child]" value="@child.Good" @(child.Good ? "checked" : "") />

                                                        </td>
                                                        <td>

                                                            <input type="radio" name="DynamicCategoryItems[@child]" value="@child.Average" @(child.Average ? "checked" : "") />
                                                        </td>
                                                        <td>

                                                            <input type="radio" name="DynamicCategoryItems[@child]" value="@child.Worse" @(child.Worse ? "checked" : "") />
                                                        </td>
                                                    </tr>
                                                </text>
                                                }

                                            </text>

                                        }

                                    </table>
                                }*@

                            @for (int i = 0; i < Model.ParentAssessments.Count; i++)
                            {
                                <text>

                                    <table>
                                        <tr>

                                            <td>
                                                @Html.HiddenFor(x => x.ParentAssessments[i].AssessmentTypeId)
                                                @Html.DisplayFor(x => x.ParentAssessments[i].AssessmentName)
                                            </td>
                                        </tr>
                                        @for (int j = 0; j < Model.ChildAssessments.Count; j++)
                                        {
                                            <text>
                                                @if (Model.ChildAssessments[i].AssessmentSubTypeId == Model.ParentAssessments[i].AssessmentTypeId)
                                                {
                                                    <text>

                                                        <tr>
                                                            <td>
                                                                @Html.HiddenFor(x=>x.ChildAssessments[i].AssessmentSubTypeId)
                                                                @Html.DisplayFor(x=>x.ChildAssessments[i].AssessmentSubTypeName)
                                                              
                                                            </td>

                                                            <td>
                                                               
                                                            </td>
                                                            
                                                        </tr>
                                                    </text>
                                                }
                                            </text>

                                        }
                                    </table>
                                </text>
                            }
                        </div>
                    </div>
                }
                @section Scripts {
                    @Scripts.Render("~/bundles/jqueryval")
                    @Scripts.Render("~/bundles/bootstrap")
                    <script type="text/javascript">
                        $(document).ready(function () {
                        });
                    </script>
                }
            </div>
        </div>
    </div>
</div>
